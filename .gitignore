# Dependencies
node_modules/
__pycache__/

# Env / secrets — ignore ALL env files; keep only the safe placeholder template
.env*
!.env.example

# Local credentials doc — never commit
LOGINS.local.md

# Build output
dist/
.vercel/

# OS / editor
.DS_Store
*.log

# Local scratch
scratchpad/

# Design source binaries (kept out of repo; Figma is source of truth)
*.pages
