# Dependencies
node_modules/

# Env / secrets — ignore ALL env files; keep only the safe placeholder template
.env*
!.env.example

# Build output
dist/
.vercel/

# OS / editor
.DS_Store
*.log

# Local scratch
scratchpad/

# Design source binaries (kept out of repo; Figma is source of truth)
*.pages
